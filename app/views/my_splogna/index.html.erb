<h1>My Needs</h1>
<table>
  <tr>
    <th>Title</th>
  </tr>
  <% @needs.each do |need| %>
    <tr>
      <td><%=link_to need.title, need_url(need) %></td>
    </tr>
  <% end %>
</table>
<%= link_to "New Need", new_need_url %>
